using UnityEngine;
//Esto ya no nos sirveXD... Iba a ser para la Mirilla... Pero, nah
public class PunteroPerfectoVR : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
